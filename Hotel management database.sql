-- Create database amazon;
-- use amazon;

-- create table rooms1(
-- room_id varchar(50),
-- room_type varchar(50),
-- price int,
-- is_available boolean,
-- is_clean boolean);

-- select*from rooms1;
-- alter TABLE rooms1 add column capacity int;

-- insert into rooms1(
-- room_id, room_type, price, is_available, is_clean)
--  value (1,'single room', 10000, true, true)
